help:
  summary: 'Profile the local network using nmap'
  has_context: false
  description: >
    Detects other hosts running on the same network as the server. The default
    script will preform a ping scan over the first external network interface.
    The network is assumed to be class-c.
